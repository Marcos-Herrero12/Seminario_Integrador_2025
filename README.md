# Sistema de Gestión de Pasantías - UTN FRC

## Instrucciones de uso:

### Iniciar Docker

Ejecutar en la terminal:

```bash
sudo systemctl start docker
sudo docker compose up -d
```

Para detener los contenedores:

```bash
docker compose down
```
